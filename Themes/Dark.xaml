<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ╔══════════════════════════════════════════════════════════════════╗
         ║  RGDSCapture — Dark Theme (Windows 11 Fluent Design)           ║
         ║  Mica-inspired layered surfaces with WinUI 3 color tokens      ║
         ╚══════════════════════════════════════════════════════════════════╝ -->

    <!-- ── WINDOW SURFACES ── -->
    <SolidColorBrush x:Key="WindowBg"           Color="#1C1C1C"/>
    <SolidColorBrush x:Key="Layer1Bg"           Color="#272727"/>
    <SolidColorBrush x:Key="Layer2Bg"           Color="#323232"/>
    <SolidColorBrush x:Key="Layer3Bg"           Color="#3D3D3D"/>
    <SolidColorBrush x:Key="VideoAreaBg"        Color="#0D0D0D"/>

    <!-- ── TOOLBAR ── -->
    <SolidColorBrush x:Key="ToolbarBg"          Color="#272727"/>
    <SolidColorBrush x:Key="ToolbarBorder"      Color="#3A3A3A"/>
    <SolidColorBrush x:Key="SeparatorBg"        Color="#444444"/>

    <!-- ── ACCENT ── -->
    <SolidColorBrush x:Key="AccentBg"           Color="#0078D4"/>
    <SolidColorBrush x:Key="AccentHover"        Color="#1585D8"/>
    <SolidColorBrush x:Key="AccentPress"        Color="#006CBE"/>
    <SolidColorBrush x:Key="AccentFg"           Color="#FFFFFF"/>

    <!-- ── STANDARD BUTTONS ── -->
    <SolidColorBrush x:Key="BtnBg"              Color="#323232"/>
    <SolidColorBrush x:Key="BtnBorder"          Color="#4A4A4A"/>
    <SolidColorBrush x:Key="BtnFg"              Color="#EFEFEF"/>
    <SolidColorBrush x:Key="BtnHover"           Color="#3D3D3D"/>
    <SolidColorBrush x:Key="BtnPress"           Color="#2A2A2A"/>

    <!-- ── ACCENT / CONNECT BUTTON ── -->
    <SolidColorBrush x:Key="BtnConnectBg"       Color="#0078D4"/>
    <SolidColorBrush x:Key="BtnConnectBorder"   Color="#0078D4"/>
    <SolidColorBrush x:Key="BtnConnectFg"       Color="#FFFFFF"/>

    <!-- ── DANGER BUTTONS ── -->
    <SolidColorBrush x:Key="BtnDangerBg"        Color="#3B1E1E"/>
    <SolidColorBrush x:Key="BtnDangerBorder"    Color="#5A2A2A"/>
    <SolidColorBrush x:Key="BtnDangerFg"        Color="#FF9090"/>
    <SolidColorBrush x:Key="BtnDangerHover"     Color="#4A2424"/>

    <!-- ── TEXT INPUTS ── -->
    <SolidColorBrush x:Key="InputBg"            Color="#222222"/>
    <SolidColorBrush x:Key="InputFg"            Color="#EFEFEF"/>
    <SolidColorBrush x:Key="InputBorder"        Color="#4A4A4A"/>
    <SolidColorBrush x:Key="InputBorderFocus"   Color="#0078D4"/>
    <SolidColorBrush x:Key="InputCaret"         Color="#EFEFEF"/>
    <SolidColorBrush x:Key="InputPlaceholder"   Color="#808080"/>

    <!-- ── TYPOGRAPHY ── -->
    <SolidColorBrush x:Key="TextPrimary"        Color="#EFEFEF"/>
    <SolidColorBrush x:Key="TextSecondary"      Color="#A8A8A8"/>
    <SolidColorBrush x:Key="TextDisabled"       Color="#585858"/>
    <SolidColorBrush x:Key="LabelFg"            Color="#CCCCCC"/>

    <!-- ── STATUS BAR ── -->
    <SolidColorBrush x:Key="StatusBarBg"        Color="#161616"/>
    <SolidColorBrush x:Key="StatusBarBorder"    Color="#303030"/>
    <SolidColorBrush x:Key="StatusFg"           Color="#A8A8A8"/>
    <SolidColorBrush x:Key="StatusDotOff"       Color="#505050"/>

    <!-- ── VIDEO PANELS ── -->
    <SolidColorBrush x:Key="ScreenBg"           Color="#080808"/>
    <SolidColorBrush x:Key="ScreenBorder"       Color="#3A3A3A"/>
    <SolidColorBrush x:Key="ScreenDivider"      Color="#181818"/>
    <SolidColorBrush x:Key="ScreenLabelFg"      Color="#383838"/>

    <!-- ── STREAM STATUS BADGES ── -->
    <SolidColorBrush x:Key="BadgeWaiting"       Color="#666666"/>
    <SolidColorBrush x:Key="FpsFg"              Color="#7A7A7A"/>

    <!-- ── LOG DRAWER ── -->
    <SolidColorBrush x:Key="SidePanelBg"        Color="#222222"/>
    <SolidColorBrush x:Key="SidePanelBorder"    Color="#383838"/>
    <SolidColorBrush x:Key="LogHeaderBg"        Color="#272727"/>
    <SolidColorBrush x:Key="LogHeaderFg"        Color="#AAAAAA"/>
    <SolidColorBrush x:Key="LogBg"              Color="#161616"/>
    <SolidColorBrush x:Key="LogFg"              Color="#4EC94E"/>

    <!-- ── SPEEDRUN TIMER ── -->
    <SolidColorBrush x:Key="TimerBg"            Color="#1A1A1A"/>
    <SolidColorBrush x:Key="TimerBorder"        Color="#3A3A3A"/>
    <SolidColorBrush x:Key="TimerFg"            Color="#F5C842"/>

    <!-- ── VU METER ── -->
    <SolidColorBrush x:Key="VuBg"               Color="#1A1A1A"/>
    <SolidColorBrush x:Key="VuBorder"           Color="#383838"/>

    <!-- ── MENU BAR ── -->
    <SolidColorBrush x:Key="MenuBg"             Color="#272727"/>
    <SolidColorBrush x:Key="MenuFg"             Color="#EFEFEF"/>
    <SolidColorBrush x:Key="MenuBorder"         Color="#3A3A3A"/>
    <SolidColorBrush x:Key="MenuItemHover"      Color="#383838"/>
    <SolidColorBrush x:Key="MenuItemPress"      Color="#444444"/>
    <SolidColorBrush x:Key="MenuSeparator"      Color="#404040"/>

    <!-- ── FOCUS RING ── -->
    <SolidColorBrush x:Key="FocusRing"          Color="#0078D4"/>

    <!-- ── COMBOBOX POPUP COLORS ── -->
    <SolidColorBrush x:Key="ComboItemFg"        Color="#EFEFEF"/>
    <SolidColorBrush x:Key="ComboItemBg"        Color="#2C2C2C"/>
    <SolidColorBrush x:Key="ComboItemHoverBg"   Color="#3D3D3D"/>
    <SolidColorBrush x:Key="ComboItemHoverFg"   Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ComboPopupBg"       Color="#272727"/>
    <SolidColorBrush x:Key="ComboPopupBorder"   Color="#4A4A4A"/>


    <!-- ═══════════════════════════════════════════════════════════════════
         COMBOBOX — full custom template so the popup respects dark colors.
         WPF's default template hardcodes SystemColors.WindowBrush (white)
         for the popup border, making text invisible in dark mode.
    ═══════════════════════════════════════════════════════════════════════ -->
    <Style TargetType="ComboBox">
        <Setter Property="Background"          Value="{DynamicResource InputBg}"/>
        <Setter Property="Foreground"          Value="{DynamicResource InputFg}"/>
        <Setter Property="BorderBrush"         Value="{DynamicResource InputBorder}"/>
        <Setter Property="BorderThickness"     Value="1"/>
        <Setter Property="Padding"             Value="10,7"/>
        <Setter Property="FontSize"            Value="13"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <!-- Selection box + arrow button -->
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                </Grid.ColumnDefinitions>
                                <!-- Selected item display -->
                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  IsHitTestVisible="False"/>
                                <!-- Editable text box (shown only when IsEditable=True) -->
                                <TextBox x:Name="PART_EditableTextBox"
                                         Grid.Column="0"
                                         Margin="{TemplateBinding Padding}"
                                         Background="Transparent"
                                         Foreground="{DynamicResource InputFg}"
                                         CaretBrush="{DynamicResource InputCaret}"
                                         BorderThickness="0"
                                         VerticalAlignment="Center"
                                         Focusable="True"
                                         Visibility="Hidden"/>
                                <!-- Drop arrow -->
                                <Path Grid.Column="1"
                                      Data="M 0 0 L 4 4 L 8 0"
                                      Stroke="{DynamicResource TextSecondary}"
                                      StrokeThickness="1.5"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsHitTestVisible="False"/>
                            </Grid>
                        </Border>

                        <!-- Popup — full dark surface, no SystemColors leakage -->
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Border x:Name="DropDownBorder"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Background="{DynamicResource ComboPopupBg}"
                                    BorderBrush="{DynamicResource ComboPopupBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                <ScrollViewer Margin="4,4">
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush"
                                    Value="{DynamicResource InputBorderFocus}"/>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="ContentSite"   Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.38"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBoxItem — dark surfaces with legible light text -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background"   Value="Transparent"/>
        <Setter Property="Foreground"   Value="{DynamicResource ComboItemFg}"/>
        <Setter Property="Padding"      Value="10,6"/>
        <Setter Property="FontSize"     Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="4"
                            Margin="3,1">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource ComboItemHoverBg}"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource ComboItemHoverFg}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource AccentBg}"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ═══════════════════════════════════════════════════════════════════
         MENU + MENUITEM — full custom templates so dropdown panels render
         with dark backgrounds. WPF's default uses SystemColors.MenuBrush
         which is always white, making text invisible against a white popup.
    ═══════════════════════════════════════════════════════════════════════ -->

    <!-- Top-level Menu bar -->
    <Style TargetType="Menu">
        <Setter Property="Background"      Value="{DynamicResource MenuBg}"/>
        <Setter Property="Foreground"      Value="{DynamicResource MenuFg}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource MenuBorder}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding"         Value="6,2"/>
        <Setter Property="FontSize"        Value="13"/>
    </Style>

    <!-- MenuItem — handles top-level header items AND submenu items -->
    <Style TargetType="MenuItem">
        <Setter Property="Background"  Value="Transparent"/>
        <Setter Property="Foreground"  Value="{DynamicResource MenuFg}"/>
        <Setter Property="FontSize"    Value="13"/>
        <Setter Property="Padding"     Value="12,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid SnapsToDevicePixels="True">

                        <!-- ── Top-level bar item (role = TopLevelHeader) ── -->
                        <Border x:Name="TopBd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="Icon"
                                                  Grid.Column="0"
                                                  ContentSource="Icon"
                                                  SnapsToDevicePixels="True"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,6,0"
                                                  Visibility="Collapsed"/>
                                <ContentPresenter Grid.Column="1"
                                                  ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"/>
                                <TextBlock x:Name="GestureText"
                                           Grid.Column="2"
                                           Text="{TemplateBinding InputGestureText}"
                                           Foreground="{DynamicResource TextDisabled}"
                                           FontSize="12"
                                           Margin="16,0,0,0"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Submenu popup — dark bg, no SystemColors leakage -->
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               HorizontalOffset="-1"
                               IsOpen="{TemplateBinding IsSubmenuOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Fade">
                            <Border x:Name="SubmenuBorder"
                                    Background="{DynamicResource MenuBg}"
                                    BorderBrush="{DynamicResource MenuBorder}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="4"
                                    SnapsToDevicePixels="True">
                                <StackPanel IsItemsHost="True"
                                            KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Show icon column when an icon is present -->
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <!-- Hide gesture text when empty -->
                        <Trigger Property="InputGestureText" Value="">
                            <Setter TargetName="GestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <!-- Hover state -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="TopBd" Property="Background"
                                    Value="{DynamicResource MenuItemHover}"/>
                        </Trigger>
                        <!-- Submenu-open = pressed appearance on the header -->
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="TopBd" Property="Background"
                                    Value="{DynamicResource MenuItemPress}"/>
                        </Trigger>
                        <!-- Submenu items use Bottom → Right placement -->
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="TopBd" Property="Padding" Value="10,7"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                            <Setter TargetName="PART_Popup" Property="HorizontalOffset" Value="0"/>
                            <Setter TargetName="PART_Popup" Property="VerticalOffset" Value="-4"/>
                            <Setter TargetName="TopBd" Property="Padding" Value="10,7"/>
                        </Trigger>
                        <!-- Checked state (checkable items) -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="TopBd" Property="Background"
                                    Value="{DynamicResource MenuItemPress}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Separator inside menus -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource MenuSeparator}"/>
        <Setter Property="Height"     Value="1"/>
        <Setter Property="Margin"     Value="6,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Fill="{TemplateBinding Background}"
                               Height="{TemplateBinding Height}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
