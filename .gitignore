# Visual Studio
.vs/
.vscode/
*.user
*.suo

# Build output
bin/
obj/

# Publish output
publish/

# Logs
*.log

# Temporary files
*.tmp

# Scratch console projects used for one-off device testing (never a real part of the app)
_harness/

# The Inno Setup script itself is SOURCE, not a build artifact — it is
# tracked so a fresh clone can rebuild the installer. Only its output
# (Output/, below) is ignored.

#block any generated, unpolished exes from getting pushed. 

Output/

#claude file
claude.md